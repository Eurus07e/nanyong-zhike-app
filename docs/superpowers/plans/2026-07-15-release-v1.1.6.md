# Nanyong Zhike v1.1.6 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the current local workspace as v1.1.6 with a Windows-safe release smoke test and all four native platform artifacts.

**Architecture:** Keep the current local v1.1.5 tree as the product baseline, preserve its uncommitted nju-cli launcher resolution fix, and add only the smallest Windows file-handle fix plus version/documentation synchronization. Publish from a new independent tag without merging `main`, PR #1, or the divergent v1.1.5 branch history.

**Tech Stack:** Python 3.12+, pytest, React/Vite, GitHub Actions, PyInstaller, Inno Setup, GitHub CLI.

---

### Task 1: Reproduce and fix the Windows SQLite handle leak

**Files:**
- Modify: `tests/test_release_smoke_assets.py`
- Modify: `desktop/smoke_test_release.py`

- [x] **Step 1: Write a failing regression test**

Add a real `sqlite3.Connection` subclass that records whether `close()` was called, monkeypatch `desktop.smoke_test_release.sqlite3.connect` to return it, create the complete release schema, call `validate_database_schema()`, and assert that the validator explicitly closes the connection.

- [x] **Step 2: Verify the test fails for the expected reason**

Run: `.venv/bin/pytest -q tests/test_release_smoke_assets.py -k closes_database_connection`

Expected: one assertion failure showing that the connection remains open after schema validation.

- [x] **Step 3: Implement the minimal fix**

Replace the transaction-only context manager in `validate_database_schema()` with an explicitly owned connection and a `try/finally` that always calls `connection.close()`.

- [x] **Step 4: Verify focused release tests pass**

Run: `.venv/bin/pytest -q tests/test_release_smoke_assets.py tests/test_desktop_launcher.py`

Expected: all selected tests pass.

### Task 2: Synchronize v1.1.6 metadata and release documentation

**Files:**
- Modify: `.github/workflows/release.yml`
- Modify: `backend/app/version.py`
- Modify: `desktop/nanyong_zhike.spec`
- Modify: `desktop/smoke_test_release.py`
- Modify: `desktop/windows-installer.iss`
- Modify: `desktop/使用说明.txt`
- Modify: `frontend/package.json`
- Modify: `frontend/package-lock.json`
- Modify: `pyproject.toml`
- Modify: `README.md`
- Modify: `tests/test_desktop_launcher.py`
- Modify: `tests/test_version_sync.py`
- Preserve: `backend/app/nju_cli.py`
- Preserve: `tests/test_nju_cli.py`

- [x] **Step 1: Change release-sensitive assertions to v1.1.6**

Update exact version and workflow-tag expectations in `tests/test_desktop_launcher.py` and `tests/test_version_sync.py`.

- [x] **Step 2: Verify version tests fail**

Run: `.venv/bin/pytest -q tests/test_version_sync.py tests/test_desktop_launcher.py`

Expected: failures report remaining v1.1.5 production metadata.

- [x] **Step 3: Update all production metadata**

Set application, package, bundle, installer, smoke-test and workflow tag versions to `1.1.6`; update README links and add a v1.1.6 changelog entry describing the Windows release fix and local nju-cli launcher resolution.

- [x] **Step 4: Verify version synchronization**

Run: `.venv/bin/pytest -q tests/test_version_sync.py tests/test_desktop_launcher.py`

Expected: all selected tests pass.

### Task 3: Run release-equivalent verification

**Files:**
- Verify only: entire tracked workspace

- [x] **Step 1: Run frontend checks**

Run: `npm run lint --prefix frontend && npm run test:unit --prefix frontend && npm run build --prefix frontend && npm audit --prefix frontend --registry=https://registry.npmjs.org`

Expected: lint/build succeed, all frontend tests pass, and audit reports zero vulnerabilities.

- [x] **Step 2: Run backend and repository checks**

Run: `NJU_CLI_BIN=/bin/true .venv/bin/pytest -q && git diff --check`

Expected: all backend tests pass and no whitespace errors are reported.

- [x] **Step 3: Validate workflow syntax and release references**

Parse both workflow YAML files and search for stale release-sensitive v1.1.5 references. Historical changelog references may remain, but executable metadata and current download links must all be v1.1.6.

### Task 4: Publish and monitor the release

**Files:**
- Append locally after completion: `交接.md` (ignored by Git)

- [ ] **Step 1: Create an independent release branch and commit**

Create `codex/release-v1.1.6` from the verified current tree, stage only intended tracked files, inspect the staged diff, and commit without merging or rebasing `main` or PR #1.

- [ ] **Step 2: Push the branch and confirm CI**

Push `codex/release-v1.1.6`, then monitor its CI run to a successful conclusion.

- [ ] **Step 3: Create and push a new annotated tag**

Create annotated tag `v1.1.6` at the verified commit and push it without moving or deleting `v1.1.5`.

- [ ] **Step 4: Monitor release jobs**

Watch the tag-triggered `Release desktop packages` workflow until verify, macOS Apple Silicon, Windows x86_64, Linux x86_64, Linux arm64 and publish all succeed.

- [ ] **Step 5: Verify published assets**

Run `gh release view v1.1.6 --repo Eurus07e/nanyong-zhike-app --json url,assets`; require macOS DMG/ZIP, Windows setup EXE/ZIP, Linux x86_64 ZIP and Linux arm64 ZIP before reporting completion.

- [ ] **Step 6: Record final handoff**

Append the commit SHA, workflow URL/result, release URL, asset list and fresh test totals to the final section of ignored `交接.md`.
