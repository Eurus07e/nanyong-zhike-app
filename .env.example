# Copy this file to .env before deployment. Never commit the real values.
COMPOSE_PROJECT_NAME=nanyong-zhike
DOMAIN=nanyong.example.com
APP_SECRET=replace-me
SESSION_TTL_HOURS=168
CLOUDFLARE_TUNNEL_TOKEN=

# The values below are only used when running the Python app outside Docker.
APP_ENV=development
COOKIE_SECURE=false
DATABASE_PATH=data/nanyong.db
NJU_CLI_BIN=
REVIEW_DATA_PATH=data/reviews/merged_data.json
